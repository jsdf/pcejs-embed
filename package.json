{
  "name": "pcejs-embed",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:embed": "mkdir -p lib/ && cp src/embed.html lib/ && browserify src/embed.js --noparse=\"node_modules/pcejs-macplus/lib/pcejs-macplus.js\" > lib/embed-bundle.js",
    "build:editor": "mkdir -p lib/ && cp src/editor.html lib/ && browserify -t babelify src/editor.js > lib/editor-bundle.js"
  },
  "author": "James Friend <james@jsdf.co> (http://jsdf.co/)",
  "license": "ISC",
  "dependencies": {
    "babelify": "^5.0.3",
    "browserify": "^4.2.3",
    "pcejs-macplus": "^0.1.5",
    "pcejs-util": "^0.1.0",
    "react": "^0.13.0-beta.1",
    "react-form-for": "^1.0.3"
  }
}
